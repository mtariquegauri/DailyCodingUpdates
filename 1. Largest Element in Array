

//User function Template for Java

class Compute {
    
    public int largest(int arr[], int n)
    {
        Arrays.sort(arr);
        // System.out.println(arr[n-1]);
        return arr[n-1];
    }
    
}
